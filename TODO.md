# TODO

## Features

- [ ] **Gitignore mode**: Add a `--gitignore` flag that reads patterns from `.gitignore` in the current working directory and deletes matching files/directories. This would allow using drclean as a quick way to purge all gitignored artifacts without manually specifying patterns.
